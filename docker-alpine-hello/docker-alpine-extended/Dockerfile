# Specify the base image
FROM alpine

# Install the curl package using the RUN directive
RUN apk add --no-cache curl

# Copy the config.txt file to the /app directory in the container
COPY config.txt /app/config.txt
